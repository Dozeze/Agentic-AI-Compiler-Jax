[project]
name = "dd2430"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "anthropic[vertex]>=1.3.0",
    "claude-agent-sdk>=0.2.151",
    "jax[cuda13]>=0.11.1",
    "python-dotenv>=1.2.3",
]

[project.scripts]
dd2430 = "dd2430:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
